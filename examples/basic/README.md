# Basic Secret Example

This example demonstrates creating a basic secret with AWS Secrets Manager.

## Features

- Simple secret creation with static value
- JSON-formatted secret content
- 30-day recovery window
- Standard configuration

## Usage

```bash
# Initialize Terraform
terraform init

# Review the plan
terraform plan

# Apply the configuration
terraform apply

# Clean up
terraform destroy
