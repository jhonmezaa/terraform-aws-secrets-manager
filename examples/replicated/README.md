# Replicated Secret Example

Multi-region secret replication with IAM policies.

## Features
- Multi-region replication (us-west-2, eu-west-1)
- Resource-based IAM policy
- Cross-account access

## Usage
```bash
terraform init
terraform plan
terraform apply
```
